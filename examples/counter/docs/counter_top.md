# counter_top

Generated by WEFT 0.5.0 from the project's sources, constraints and reports. Regenerate it rather than editing it.

eight-bit demonstration counter driving LEDs and a display

Holding both buttons at once clears the counter. The display blanks while the counter is paused, so a stopped count cannot be mistaken for a slow one.

The hierarchy spans three languages on purpose: this module and the debouncer and counter are SystemVerilog, clk_tick is Verilog-2001, and seven_seg_decoder is VHDL. Any tool that claims to index or document this project has to cross those boundaries.

Defined in `counter/src/counter_top.sv:24` (systemverilog).

## Parameters

| Name | Type | Default | Description |
|---|---|---|---|
| CLK_HZ | int | 50_000_000 | board clock frequency in Hz |
| TICK_HZ | int | 4 | how many steps per second the counter takes |
| STABLE_MS | int | 10 | debounce window for the push buttons |

## Ports

| Name | Direction | Type | Description |
|---|---|---|---|
| clk | in | logic | board clock, rising edge active |
| rst_n | in | logic | asynchronous reset, active low |
| btn_n | in | logic [1:0] | two push buttons, active low: [0] direction, [1] run/pause |
| led | out | logic [7:0] | the counter value |
| seg_n | out | logic [6:0] | low nibble of the counter on a seven-segment display |
| heartbeat | out | logic | 1 Hz blink, evidence that the clock is alive |

## Instances

| Instance | Module | Line |
|---|---|---|
| u_btn_dir | debouncer | 57 |
| u_btn_run | debouncer | 68 |
| u_tick | clk_tick | 100 |
| u_count | updown_counter | 110 |
| u_hb | clk_tick | 125 |
| u_seg | seven_seg_decoder | 137 |

## Instantiated by

- counter_top_tb
